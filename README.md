# some-algorithms-of-list
There are create list ,sort list,merge list print list algorithms.
The ideas is effecitve.
the struct:
struct ListNode
{
    int val;
    struct ListNode *next;
    ListNode(int x):val(x),next(NULL){}
}
